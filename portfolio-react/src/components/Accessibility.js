import './Accessibility.css';

function Accessibility({name}) {
    return (
        <figcaption className="accessibility">
            {name}
        </figcaption>
    )   
};

export default Accessibility;